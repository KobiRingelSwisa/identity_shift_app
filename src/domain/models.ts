/**
 * Domain types (re-exported from persistence layer; single source in `backend/types` until full split).
 */
export type {
  ProgramProgressSnapshot,
  SaveAnchorPayload,
  SessionRun,
  SessionStepRun,
  UserProfile,
} from '../backend/types';
